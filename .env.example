SERVER_URL=127.0.0.1:7799

ORACLE_SSE=https://oracle.zoroswap.com/v1/updates/price/stream
ORACLE_HTTPS=https://oracle.zoroswap.com/v1/updates/price/latest

# Miden testnet (default). On first run, use a fresh store — delete store.sqlite3 if migrating from devnet.
MIDEN_NETWORK=testnet
TX_PROVER_URL=https://tx-prover.testnet.miden.io
TX_PROVER_TIMEOUT_SECS=30
